{% extends 'base.html.twig' %}

{% block title %}Autorisation{% endblock %}

{% block body %}
    <h1>Autorisation</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td><span class="fw-normal">{{ autorisation.id }}</span></td>
            </tr>
            <tr>
                <th>Date</th>
                <td><span class="fw-normal">{{ autorisation.date ? autorisation.date|date : '' }}</span></td>
            </tr>
            <tr>
                <th>Status</th>
                <td><span class="fw-normal">{{ autorisation.status ? 'Yes' : 'No' }}</span></td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_autorisation_index') }}

    <a href="{{ path('app_autorisation_edit', {'id': autorisation.id}) }}"><button class="btn btn-danger me-2">edit</button></a>

    {{ include('autorisation/_delete_form.html.twig') }}
{% endblock %}
