<form method="post" action="{{ path('app_group_employer_delete', {'id': group_employer.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ group_employer.id) }}">
    <button class="btn">Delete</button>
</form>
