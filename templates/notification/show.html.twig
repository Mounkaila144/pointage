{% extends 'base.html.twig' %}

{% block title %}Notification{% endblock %}

{% block body %}
    <h1>Notification</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td><span class="fw-normal">{{ notification.id }}</span></td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_notification_index') }}

    <a href="{{ path('app_notification_edit', {'id': notification.id}) }}"><button class="btn btn-danger me-2">edit</button></a>

    {{ include('notification/_delete_form.html.twig') }}
{% endblock %}
