{% extends 'base.html.twig' %}

{% block title %}Admin{% endblock %}

{% block body %}
    <h1>Admin</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td><span class="fw-normal">{{ admin.id }}</span></td>
            </tr>
            <tr>
                <th>Email</th>
                <td><span class="fw-normal">{{ admin.email }}</span></td>
            </tr>
            <tr>
                <th>Roles</th>
                <td><span class="fw-normal">{{ admin.roles ? admin.roles|json_encode : '' }}</span></td>
            </tr>
            <tr>
                <th>Password</th>
                <td><span class="fw-normal">{{ admin.password }}</span></td>
            </tr>
            <tr>
                <th>Nom</th>
                <td><span class="fw-normal">{{ admin.nom }}</span></td>
            </tr>
            <tr>
                <th>Prenom</th>
                <td><span class="fw-normal">{{ admin.prenom }}</span></td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_adminform_index') }}

    <a href="{{ path('app_adminform_edit', {'id': admin.id}) }}"><button class="btn btn-danger me-2">edit</button></a>

    {{ include('adminform/_delete_form.html.twig') }}
{% endblock %}
